import { SelectItemsDirective } from './select-items.directive';

describe('SelectItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectItemsDirective();
    expect(directive).toBeTruthy();
  });
});
